//BBOalert, Bidding Scenarios Toggle
//BBOalert, Bidding Scenarios Toggle - version 4.1.11
//
// RETIRED. This variant is no longer maintained separately: it was a stale copy
// of release (v4.1.8, still on the pre-polling js/setDealerCode.js) that missed
// every fix applied to the other two files. Rather than delete it - a PBSCache
// is seeded once and persists, so a removed URL would 404 forever for anyone
// still pointed here - it now loads exactly what release loads. A stale cache
// therefore upgrades itself instead of breaking.
//
// If you are looking for where to make a change, make it in -PBS.txt.
//
// RELEASE channel. The JavaScript this file used to carry inline now lives in
// runtime/ in bridge-craftwork/pbs-bbo-extension, fetched at run time. Release
// imports it from that repo's `release` branch and beta imports the same paths
// from `main`, so the two channels are one body of code at two revisions and
// promoting beta is a merge rather than a copy.
//
// Do NOT add the dev paste-loader here; it is beta-only.
//
// PBSCache is seeded once in a user's localStorage and persists, so this file
// must remain the entry point at this URL forever.
Import,https://github.com/bridge-craftwork/pbs-bbo-extension/blob/release/runtime/setDealerCode-polling.js
Import,https://github.com/bridge-craftwork/pbs-bbo-extension/blob/release/runtime/toggleRotate.js
Javascript,https://github.com/stanmaz/BBOalert/blob/master/Plugins/PBNcapture.js
Javascript,https://github.com/bridge-craftwork/bbo-pbs/blob/master/Plugins/BBAcompare.js
Import,https://github.com/stanmaz/BBOalert/blob/master/Scripts/PBStooltips.js

// Channel identity. runtime/ is shared by every channel and carries no version
// of its own, so the data file - the one per-channel artefact - declares it.
//
// The two numbers below are different things and move independently:
//
//   pbsVersionLabel   The PBS package version - the same number as line 2 of
//                     this file. Printed in the console banner on startup:
//                     "PBS <label>: Initializing...". Human-facing, and the
//                     first thing anyone reads in a bug report.
//
//   pbsClientVersion  The plug-in version - the same number BBAcompare.js
//                     carries as CLIENT_VERSION. Sent as the X-Client-Version
//                     header to bba.harmonicsystems.com/api/scenario/select on
//                     EVERY scenario click. Machine-facing: this is what the
//                     telemetry counts, and what tells release traffic apart
//                     from beta and toggle.
//
// Keep them in step with line 2 and with BBAcompare respectively. runtime/
// falls back to "unset" rather than to a real version, so forgetting either
// shows up as obviously missing instead of as a wrong channel.
// The -toggle suffix is deliberate and load-bearing: it is POSTed as
// X-Client-Version on every scenario click, so whether anyone is still pointed
// at this file becomes a thing you can look up rather than a thing you guess.
// If nothing reports -toggle for a good while, this file can be left to rot
// safely - though still not deleted.
Script,onDataLoad,window.pbsVersionLabel='v4.1.11-toggle'; window.pbsClientVersion='1.9.26-toggle'; R='';

// Host-conflict guard - must load before the blocks that call it.
Import,https://github.com/bridge-craftwork/pbs-bbo-extension/blob/release/runtime/deferGuard.js

// Handle BBO tabs that appear after startup (the "Tables" tab).
Import,https://github.com/bridge-craftwork/pbs-bbo-extension/blob/release/runtime/tabDelegation.js

// One active table-watcher at a time when both extensions are installed.
// This only prevents the freeze when BOTH data files import it, which is why it
// has to be here and not only in -PBS-beta.txt.
Import,https://github.com/bridge-craftwork/pbs-bbo-extension/blob/release/runtime/activeWatcher.js

// Display HCP for visible hands
Import,https://github.com/bridge-craftwork/pbs-bbo-extension/blob/release/runtime/displayHCP.js

Import,https://github.com/bridge-craftwork/pbs-bbo-extension/blob/release/runtime/onAnyMutation-hcp.js

// Start BBA Auction Compare panel (called by Auction Compare button)
Script,startBBACompare,window.startBBACompare(); R='';
Script,toggleRotate,window.toggleRandomlyRotate(); R='';

// Pause
Script,onLogin,setTimeout(setOptionsOff, 200);

// Open BBOalert Shortcuts
Script,onDataLoad,$('#bttab-buttons').click();

// Open usual BBO side panel
Script,onLogin,parent.$('.verticalClass')[0].click();

// Remove the Char, Word, All backspace buttons (hide isn't enough — master toggle re-shows white buttons)
Script,onDataLoad,$("#adpanel2 button:lt(3)").remove();

// Fix button row heights - make adjacent buttons equal height
Import,https://github.com/bridge-craftwork/pbs-bbo-extension/blob/release/runtime/panelStyle.js

// Redirect chat so it doesn't go to Lobby
Import,https://github.com/bridge-craftwork/pbs-bbo-extension/blob/release/runtime/chatDestination.js

Import,https://github.com/bridge-craftwork/pbs-bbo-extension/blob/release/runtime/startTable.js

Script,setBiddingTable,localStorage.setItem('pbsLastTableType','bidding'); window.startTable('bidding'); R='';
Script,setTeachingTable,localStorage.setItem('pbsLastTableType','teaching'); window.startTable('teaching'); R='';


Script,reviewCurrentCode,setDealerCode("");

Option,Practice Table

// Dynamic Layout System v4.1
// Fetches layout configuration and PBS metadata at runtime
// Rebuilds buttons immediately when config changes
Import,https://github.com/bridge-craftwork/pbs-bbo-extension/blob/release/runtime/pbsDynamicLayout.js

// Config change detection - rebuilds buttons when settings change
Import,https://github.com/bridge-craftwork/pbs-bbo-extension/blob/release/runtime/onAnyMutation-config.js

Option
