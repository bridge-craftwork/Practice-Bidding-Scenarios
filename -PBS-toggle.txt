//BBOalert, Bidding Scenarios Toggle
//
// REDIRECT ONLY - this branch holds no content of its own.
//
// split/beta-runtime was the development branch for the runtime/ split, and
// while that work was in flight a beta tester's PBSCache was pointed at THIS
// branch's URL: the git branch was acting as the channel.
//
// It does not need to any more. The channel is a property of the FILE now
// (-PBS.txt imports runtime/ from `release`, -PBS-beta.txt from `main`), so
// both live side by side on main and main is the only place to edit.
//
// The branch is kept because a PBSCache is seeded once and persists: deleting
// it would 404 forever for anyone still pointed here, with no way to recover.
// Keeping a COPY here instead would be worse - it would go stale the first time
// somebody edited main and forgot this branch, which is exactly the trap that
// bit twice during the split. A redirect cannot go stale, because it has
// nothing to go stale.
//
// Do not add content below. Edit main.
Import,https://github.com/bridge-craftwork/Practice-Bidding-Scenarios/blob/main/-PBS-toggle.txt
